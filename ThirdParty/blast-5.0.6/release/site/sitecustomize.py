
# THIS FILE IS AUTOGENERATED BY repo_build
import sys
import os
p = os.path.abspath(os.path.join(os.path.dirname(__file__), '..'))
if hasattr(os, "add_dll_directory"):
    add_library_path = os.add_dll_directory
else:
    def add_library_path(p):
        os.environ["PATH"] += os.pathsep + p                    
if os.path.exists(f"" + os.getenv("PATH", default="") + ""):
    add_library_path(f"" + os.getenv("PATH", default="") + "")
if os.path.exists(f"{p}/../../../" + os.getenv("PATH", default="") + ""):
    add_library_path(f"{p}/../../../" + os.getenv("PATH", default="") + "")
if os.path.exists(f"{p}/."):
    add_library_path(f"{p}/.")
if os.path.exists(f"" + os.getenv("PYTHONPATH", default="") + ""):
    sys.path.append(f"" + os.getenv("PYTHONPATH", default="") + "")
if os.path.exists(f"{p}/../../../" + os.getenv("PYTHONPATH", default="") + ""):
    sys.path.append(f"{p}/../../../" + os.getenv("PYTHONPATH", default="") + "")
if os.path.exists(f"{p}/../../../../../../../../../packman-repo/packman-common/7.10.1"):
    sys.path.append(f"{p}/../../../../../../../../../packman-repo/packman-common/7.10.1")
if os.path.exists(f""):
    sys.path.append(f"")
if os.path.exists(f"{p}/../../.."):
    sys.path.append(f"{p}/../../..")
